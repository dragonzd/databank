// Generated by Trae Work
(function() {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();
  var emerald = '#10b981';
  var amber = '#f59e0b';
  var purple = '#a78bfa';
  var red = '#ef4444';

  // --- Chart 1: 10 Industry × 3 Role Heatmap ---
  var chart1 = echarts.init(document.getElementById('chart-domain-distribution'), null, { renderer: 'svg' });

  var industries = ['出行', '教育', '医疗', '养老', '生活', '生意', '生产制造', '社会信用', '环境治理', '科研创新'];
  var roles = ['个人', '企业', '政务'];

  // [industry index, role index, count]
  // Count = existing scenarios + new scenarios in that cell
  var data = [
    [0, 0, 5], [0, 1, 3], [0, 2, 2],
    [1, 0, 5], [1, 1, 3], [1, 2, 3],
    [2, 0, 5], [2, 1, 3], [2, 2, 3],
    [3, 0, 3], [3, 1, 4], [3, 2, 2],
    [4, 0, 5], [4, 1, 3], [4, 2, 2],
    [5, 0, 3], [5, 1, 5], [5, 2, 2],
    [6, 0, 1], [6, 1, 5], [6, 2, 2],
    [7, 0, 2], [7, 1, 3], [7, 2, 2],
    [8, 0, 1], [8, 1, 2], [8, 2, 3],
    [9, 0, 2], [9, 1, 2], [9, 2, 2]
  ];

  chart1.setOption({
    animation: false,
    tooltip: {
      position: 'top',
      appendToBody: true,
      formatter: function(p) {
        return industries[p.value[0]] + ' · ' + roles[p.value[1]] + '<br/>场景数: ' + p.value[2] + ' 个';
      }
    },
    grid: { left: '12%', right: '8%', top: '5%', bottom: '8%' },
    xAxis: {
      type: 'category',
      data: roles,
      axisLabel: { color: muted, fontSize: 13, fontWeight: 'bold' },
      axisLine: { lineStyle: { color: rule } },
      axisTick: { show: false },
      splitArea: { show: false }
    },
    yAxis: {
      type: 'category',
      data: industries,
      axisLabel: { color: ink, fontSize: 12, fontWeight: 'bold' },
      axisLine: { lineStyle: { color: rule } },
      axisTick: { show: false },
      splitArea: { show: false }
    },
    visualMap: {
      min: 0,
      max: 5,
      calculable: false,
      orient: 'horizontal',
      left: 'center',
      bottom: '0%',
      inRange: {
        color: [bg2, accent2 + '66', accent2 + 'aa', accent]
      },
      text: ['多', '少'],
      textStyle: { color: muted }
    },
    series: [{
      type: 'heatmap',
      data: data,
      label: {
        show: true,
        fontSize: 14,
        fontWeight: 'bold',
        color: ink
      },
      emphasis: {
        itemStyle: {
          shadowBlur: 10,
          shadowColor: 'rgba(0,0,0,0.5)'
        }
      }
    }]
  });
  window.addEventListener('resize', function() { chart1.resize(); });

  // --- Chart 2: Priority Matrix (Scatter) ---
  var chart2 = echarts.init(document.getElementById('chart-priority-matrix'), null, { renderer: 'svg' });
  var scenarios = [
    { name: '运动健康', x: 95, y: 95, itemStyle: { color: emerald }, label: 'P0' },
    { name: '金融风控', x: 85, y: 90, itemStyle: { color: emerald }, label: 'P0' },
    { name: '企业竞争力', x: 80, y: 88, itemStyle: { color: emerald }, label: 'P0' },
    { name: '社会信用', x: 78, y: 85, itemStyle: { color: emerald }, label: 'P0' },
    { name: '新能源车', x: 72, y: 78, itemStyle: { color: emerald }, label: 'P0' },
    { name: '街区分析', x: 70, y: 75, itemStyle: { color: emerald }, label: 'P0' },
    { name: '教育评估', x: 72, y: 76, itemStyle: { color: amber }, label: 'P1' },
    { name: '学习场景', x: 70, y: 72, itemStyle: { color: amber }, label: 'P1' },
    { name: '社区环境', x: 68, y: 70, itemStyle: { color: amber }, label: 'P1' },
    { name: '服装定制', x: 65, y: 68, itemStyle: { color: amber }, label: 'P1' },
    { name: '设备采购', x: 68, y: 70, itemStyle: { color: amber }, label: 'P1' },
    { name: '药品研发', x: 60, y: 65, itemStyle: { color: amber }, label: 'P1' },
    { name: '养老规划', x: 62, y: 72, itemStyle: { color: amber }, label: 'P1' },
    { name: '风险预警', x: 55, y: 80, itemStyle: { color: red }, label: 'P2' },
    { name: '农业优化', x: 55, y: 55, itemStyle: { color: purple }, label: 'P2' },
    { name: '家政匹配', x: 50, y: 52, itemStyle: { color: purple }, label: 'P2' },
    { name: '人才招聘', x: 52, y: 50, itemStyle: { color: purple }, label: 'P2' },
    { name: '旅游规划', x: 48, y: 48, itemStyle: { color: purple }, label: 'P2' },
    { name: '保险精算', x: 55, y: 58, itemStyle: { color: purple }, label: 'P2' },
    { name: '建筑设计', x: 45, y: 45, itemStyle: { color: purple }, label: 'P2' },
    { name: '营养膳食', x: 50, y: 55, itemStyle: { color: purple }, label: 'P2' },
    { name: '二手估值', x: 48, y: 45, itemStyle: { color: purple }, label: 'P2' },
    { name: '碳排放', x: 50, y: 52, itemStyle: { color: purple }, label: 'P2' }
  ];

  chart2.setOption({
    animation: false,
    tooltip: {
      trigger: 'item',
      appendToBody: true,
      formatter: function(p) { return p.name + '<br/>平台就绪度: ' + p.value[0] + '%<br/>商业价值: ' + p.value[1] + '%'; }
    },
    grid: { left: '8%', right: '4%', bottom: '8%', top: '8%' },
    xAxis: {
      name: '平台就绪度 (%)',
      nameTextStyle: { color: muted },
      min: 30, max: 110,
      axisLabel: { color: muted },
      splitLine: { lineStyle: { color: rule } },
      axisLine: { lineStyle: { color: rule } }
    },
    yAxis: {
      name: '商业价值 (%)',
      nameTextStyle: { color: muted },
      min: 30, max: 110,
      axisLabel: { color: muted },
      splitLine: { lineStyle: { color: rule } },
      axisLine: { lineStyle: { color: rule } }
    },
    series: [{
      type: 'scatter',
      data: scenarios.map(function(s) { return { value: [s.x, s.y], name: s.name, itemStyle: s.itemStyle, label: s.label }; }),
      symbolSize: 14,
      label: {
        show: true,
        formatter: function(p) { return p.name; },
        position: 'right',
        fontSize: 10,
        color: muted
      },
      markLine: {
        silent: true,
        lineStyle: { color: rule, type: 'dashed' },
        data: [
          { xAxis: 65, label: { formatter: '就绪度阈值', color: muted } },
          { yAxis: 65, label: { formatter: '价值阈值', color: muted } }
        ]
      }
    }]
  });
  window.addEventListener('resize', function() { chart2.resize(); });
})();